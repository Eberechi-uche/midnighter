import { Button } from "@chakra-ui/react";

type PaginationProps = {
  list: [];
};

const Pagination: React.FC<PaginationProps> = ({ list }) => {
  return (
    <>
      <Button></Button>
    </>
  );
};
